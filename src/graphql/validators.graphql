query Validators {
  validator_status {
    height
    jailed
    status
    validator_address
    validator {
      validator_descriptions {
        identity
        avatar_url
        details
        moniker
        website
      }
      validator_info {
        self_delegate_address
      }
    }
  }
}
