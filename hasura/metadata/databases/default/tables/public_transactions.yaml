table:
  name: transactions
  schema: public
configuration:
  column_config: {}
  custom_column_names: {}
  custom_root_fields:
    delete_by_pk: delete_transaction
    insert_one: insert_transaction
    select_by_pk: transaction
    update_by_pk: update_transaction
object_relationships:
- name: block
  using:
    foreign_key_constraint_on: height
